import styled from "styled-components";
export const PosterWrapper = styled.figure`
    filter:brightness(110%) saturate(40%) contrast(110%);
    height:55rem;
    margin:auto;
    max-width:95%;
    position:relative;
    width:45rem;
`