import styled from 'styled-components';
export const ProjectsWrapper = styled.ul`
    align-items:center;
    display:flex;
    flex-flow:row wrap;
    justify-content:center;
    gap:1rem;
    padding:.85rem .25rem;
`